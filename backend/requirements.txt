# Web framework
fastapi==0.110.0
uvicorn[standard]==0.29.0
#pydantic==2.7.1  # Already included in Utilities section

# Data / ML
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
xgboost==2.0.3
joblib==1.4.2
matplotlib==3.8.4

# LLM
# Ollama is used through its local HTTP API, so no Python LLM runtime is required.
# Install transformers/torch separately only if LLM_PROVIDER=huggingface is needed.

# Database
pymongo==4.6.1
motor==3.3.2

# Utilities
pydantic==2.7.1
python-dotenv==1.0.1

# Auth
python-jose[cryptography]
passlib==1.7.4
PyJWT 
cryptography

#pwdlib argon
pwdlib[argon2]

python-multipart
requests==2.31.0

websockets
# For network capture (if needed)
scapy
httpx
